import 'package:flutter/material.dart';

class Lec32Part5Screen extends StatelessWidget {
  const Lec32Part5Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Lecture3.2 Part5 Screen'),
        centerTitle: false,
        //leading: const Icon(Icons.menu),
      ),
      body: const Column(children: []),
    );
  }
}
